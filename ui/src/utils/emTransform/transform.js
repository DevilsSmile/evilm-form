export default function transform (emformRaw, emformRule) {
    let key = Object.keys(emformRaw)
    let value = Object.values(emformRaw)
    let rule = Object.values(emformRule)

    for (let i = 0, l = key.length; i < l; i++) {
        if (emformRule[key[i]]) {
            
        }
    }
}